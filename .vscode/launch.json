{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "xcode-project",
      "type": "Ruby",
      "request": "launch",
      "cwd": "${workspaceRoot}",
      "program": "${file}",
      "args": [
        "--workspace=../login",
        "--target=LoginSlice",
        "--command=1",
        "--sync"
      ]
    }
  ]
}